function setup() {
  createCanvas(windowWidth, windowHeight);
  colorMode(HSB);
  angleMode(DEGREES);
}

function draw() {
  background(120,100,100); //lime green background

  circle(100,100,100);
  square(175,50,100);

}
